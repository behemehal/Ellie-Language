#![allow(warnings)]

use crate::mapper::Collecting;

struct RawUtils {
    pub ellie_version: (i64,i64,i64,i64)
}

impl RawUtils {
    fn convert_to_raw(element: Collecting) -> String {
        let returned_string = "";

        


        returned_string.to_string()
    }

    fn convert_from_raw()  {
        
    }
}