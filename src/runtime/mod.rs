/*
use crate::error;
use crate::mapper;
use crate::processors;
use crate::syntax::{function, types, variable};
use crate::utils;

#[derive(PartialEq, Debug, Clone)]
pub struct Runtime {
    pub mapped_code: mapper::Mapped,
}

impl Runtime {
    pub fn new(mapped_code: mapper::Mapped) -> Self {
        Runtime {
            mapped_code
        }
    }

    pub fn create_thread() {
        
    }
}
*/