/*
use crate::error;
use crate::parser;
use crate::processors;
use crate::syntax::{function, types, variable};
use crate::utils;

#[derive(PartialEq, Debug, Clone)]
pub struct Runtime {
    pub mapped_code: parser::Mapped,
}

impl Runtime {
    pub fn new(mapped_code: parser::Mapped) -> Self {
        Runtime {
            mapped_code
        }
    }

    pub fn create_thread() {
        
    }
}
*/