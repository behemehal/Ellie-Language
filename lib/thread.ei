import error;
import int;
import function;
import string;

pub class callStack {
    pub v location: cursorPosition;
    pub v path: string;
} 

pub fn panic(error: error);
pub fn sleep(ms: int);
pub fn create_thread(inside: @());