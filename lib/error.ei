import int;
import string;

pub class CursorPosition {
    co(line, column);
    pub v line : int;
    pub v column : int;
}

pub class Error {
    co(code, title, message);

    pub v code: int;
    pub v title: int;
    pub v message: int;
    pub v path: string;
    pub v pos: CursorPosition;
}